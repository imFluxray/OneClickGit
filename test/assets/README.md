# App icon source

Place your square logo here, e.g. `icon.png` (1024×1024 recommended), then run from the project root:

```bash
npm run tauri icon assets/icon.png
npm run tauri build
```

Generated icons are written to `src-tauri/icons/`.
